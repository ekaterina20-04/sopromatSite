services:
  frontend:
    build: ./KateDiplom
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    expose:
      - "3001"
